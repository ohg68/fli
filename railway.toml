[build]
builder = "NIXPACKS"
nixpacksPlan = { "repo" = "punitarani/fli" }

[deploy]
numReplicas = 1
startCommand = "uv run python railway_start.py" 
sleepApplication = true
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
