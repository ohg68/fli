[build]
builder = "NIXPACKS"
nixpacksPlan = { "repo" = "punitarani/fli" }

[deploy]
numReplicas = 1
startCommand = "HOST=0.0.0.0 PORT=8080 uv run fli-mcp-http"
sleepApplication = true
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
